// Top-up modal removed with the Klap system. Stub kept temporarily so existing
// imports compile while ProposalModal/Dashboard are being rewritten.
// TODO: delete once no component imports TopUpModal.
export const TopUpModal = ({ open }: { open: boolean; onClose: () => void }) => {
  if (!open) return null;
  return null;
};
